责任链模式
是将链中每一个节点看做是一个对象，每个节点处理的请求均不同，且内部自动维护一个下一个节点对象
当每一个请求从链式的首端发出时，会沿着链的路径依次传递给每一个节点对象
直至有对象处理这个请求位置

生活中的应用
审批流程


适用场景
多个对象处理同一个请求，但具体哪个对象处理则在运行时动态决定
在不明确指定接受者的情况下，像多个对象中的一个提交一个请求
可以动态指定一组对象处理请求


优点
将请求和处理解耦
请求矗立着只需要关注自己感兴趣的请求进行处理即可


缺点
责任链太长或者处理时间太长会影响整体性能
如果节点对象存在循环引用时，会造成死循环
